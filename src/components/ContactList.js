import React from 'react';

export const ContactList = () => {
  {
    /* <ul>
            {contacts.map(({ name, id, number }) => {
              return (
                <li key={id}>
                  {name}: {number}
                </li>
              );
            })}
          </ul> */
  }
};
